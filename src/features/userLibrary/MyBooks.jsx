import Profile from '../auth/Profile';

// MyBooks is a full alias for Profile — shows the user's book collection.
// Accessed via /my-books route, same component as /profile for now.
const MyBooks = () => <Profile />;

export default MyBooks;
